import sqlite3
import collection

class SqliteDict(collection.MutableMapping):
    
    def __init__(self, filename):
        self.
        
    def update(self, )
        with self.connection:
            cursor = self.connection.cursor()


url_path, file_last_updated, file_hash, os_path

# initialization



# create table (if it doesn't exist?)
# for all files



# when adding new row, delete all other 